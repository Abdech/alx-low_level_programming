#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 *rev_sting-  This function prints to the stdout
 *
 *@str: is the parameter that prints using puts
 *
 *Return: its a void func
 */
void rev_string(char *s)
{
	int count = 0, int i, int len;
	char holder
	
	while (s[count] != '\0')
		count++;
	len = count -1;

	for (i = 0; i < count / 2 ; i++)

}
