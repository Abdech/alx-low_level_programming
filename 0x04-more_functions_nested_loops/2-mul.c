#include <ctype.h>
#include <stdio.h>
#include "main.h"

/**
 *mul - multiply the two arguments
 *
 *@a: is the input parameters that is multiplied by @b
 *@b: is the input parameters that is multipled by a arg
 * Return: The multiplied argst
 */
int mul(int a, int b)
{
	return (a * b);
}
