#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 *_puts-  This function prints to the stdout
 *
 *@str: is the parameter that prints using puts
 *
 *Return: its a void func
 */
void _puts(char *str)
{
	puts(str); /*writing the string from input parameter to stdout*/
}
